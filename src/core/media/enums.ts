export enum EMediaErrorKind {
  unsupported = 'unsupported',
  notAllowed = 'not_allowed',
  notFound = 'not_found',
  overconstrained = 'overconstrained',
  notReadable = 'not_readable',
  unknown = 'unknown',
}

export enum EMediaKind {
  videoAndAudio = 'video_and_audio',
  audio = 'audio',
}
